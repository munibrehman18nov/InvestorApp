<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.investorapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/investlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/investlogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".iCategory1" />
        <activity android:name=".iCategory2" />
        <activity android:name=".iCategory3" />
        <activity android:name=".DriversActivity" />
        <activity android:name=".VehiclesActivity" />
        <activity android:name=".AddDriverActivity" />
        <activity android:name=".DeleteDriverActivity" />
        <activity android:name=".AddVehicleActivity" />
        <activity android:name=".DeleteVehicleActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" /> <!-- optional (needed if default theme has no action bar) -->
        <activity android:name=".DProfileActivity" />
        <activity android:name=".SendEmail"></activity>
    </application>

</manifest>